import 'package:flutter/material.dart';

class VoiceTranscriptionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Voice Transcription", style: TextStyle(fontSize: 24)),
    );
  }
}
