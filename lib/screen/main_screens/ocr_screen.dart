import 'package:flutter/material.dart';

class OCRScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("OCR Processing", style: TextStyle(fontSize: 24)),
    );
  }
}
